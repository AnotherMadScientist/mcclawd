//! McClawd API — library entrypoint for integration tests.
//!
//! The primary entrypoint is the `mc` binary (`main.rs`).
//! This `lib.rs` re-exports modules that integration tests need.

pub mod sandbox;
